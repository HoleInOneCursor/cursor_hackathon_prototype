# cursor_hackathon_prototype

## Agent helper

Run the lightweight repo briefing script when an agent (or human) needs quick
orientation:

```bash
bash scripts/agent-brief.sh
```

It prints the current branch, upstream, working-tree changes, recent commits,
top-level tracked files, and a few safe next commands without requiring any
project dependencies.
